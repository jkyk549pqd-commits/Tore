/*
 * 所有常量入口
 *
 */
import menu from './system/menu-const';
import { FLAG_NUMBER_ENUM, USER_TYPE_ENUM } from './common-const';
import { LAYOUT_ENUM } from './layout-const';

export default {
  FLAG_NUMBER_ENUM,
  USER_TYPE_ENUM,
  LAYOUT_ENUM,
  ...menu
};
